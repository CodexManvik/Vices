# chat.py
# Intentionally disabled.
# This file is an interactive CLI that targets a different endpoint
# (`/completion`) and embeds its own prompt logic.
#
# The FastAPI server (`server.py`) is the active entrypoint for the AI companion.
